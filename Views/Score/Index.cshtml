@model IEnumerable<EverUnited.Models.Match_Scorer>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout_team.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Player.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Match.venue)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.score)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.score_description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.assist_player_id)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Player.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Match.venue)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.score)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.score_description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.assist_player_id)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
